<h1>Cocktails</h1>

<ul>
  <% @cocktails.each do |cocktail| %>
    <li><%= link_to "#{cocktail.name}", cocktail_path(cocktail) %></li>
  <% end %>
</ul>

<%= link_to '<i class="fas fa-plus"></i> Add a new cocktail!'.html_safe, new_cocktail_path %>

